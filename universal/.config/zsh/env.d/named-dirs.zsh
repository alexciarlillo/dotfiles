# Named directory hashes (~vault, ~agent, ...)
# Defined here (sourced by .zshenv) so they resolve in non-interactive shells too.
hash -d vault="$HOME/Documents/Alex's Vault/"
hash -d agent="$HOME/GitHub/roblox/.agent-meta/"
